# configs/train.yaml
defaults:
  - data/tusimple  # pulls in configs/data/tusimple.yaml

train:
  batch_size: 2
  num_workers: 0     # set >0 later (Windows can keep 0 for now)
  epochs: 1          # placeholder

model:
  name: unet_mobilenetv2  # placeholder, used later

